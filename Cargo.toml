[package]
name = "draft"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["time", "sync", "macros"] }
thiserror = "2"

[dev-dependencies]
tokio = { version = "1", features = ["full", "test-util"] }
