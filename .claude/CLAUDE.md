# 规范

1. 使用中文对话。
2. 代码注释也要使用中文。
3. 机制要使用术语说明，术语的定义要写入文档。对话过程使用到了关于项目内的新的术语也要写进文档。
4. 【强制】rust故障处理，src生产代码不允许使用unwrap，panic！，example和test代码不强制不能使用。
5. 不要使用魔数，应通过定义常量和传入参数来实现。
6. 【强制】有规划文档时，在进行代码改动前，先修改文档，防止文档和代码对不上。
7. 在实现公共库之前，必须先定义好api接口到规划文档里经过审查。
